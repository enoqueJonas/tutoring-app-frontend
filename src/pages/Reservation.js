import React from 'react';
import ReservationForm from '../components/ReservationForm';

const Reservation = () => (
  <div className="login-page-wrapper items-center flex bg-lime-500 h-screen w-screen overflow-hidden justify-center absolute z-50">
    <ReservationForm />
  </div>
);

export default Reservation;
