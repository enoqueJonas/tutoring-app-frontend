import React from 'react';
import LoginFields from '../components/LoginFields';

const Login = () => (
  <div className="login-page-wrapper mainContainer items-center flex bg-lime-500 h-screen w-screen overflow-hidden justify-center absolute z-50">
    <LoginFields />
  </div>
);

export default Login;
